# ⚠️ SECURITY WARNING - NEVER COMMIT .env FILE ⚠️
# Always keep .env in .gitignore and use this file as a template

# Database (Neon) - Get from https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT Secret - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-secret-key-here

# Mada Integration (Supabase) - Optional
MADA_SUPABASE_URL=https://your-project.supabase.co
MADA_SUPABASE_KEY=your-service-role-key

# Vetor Imobi Integration (Base44 API)
VETOR_API_KEY=your-vetor-api-key
VETOR_BASE_URL=https://app.base44.com/api/apps/687412c6a963138588a2720b

# Neon Auth - Optional
NEON_AUTH_BASE_URL=https://your-neon-auth-url.neon.tech/neondb/auth

# Google OAuth - Optional
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# Server
PORT=3001
NODE_ENV=development

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# Cron Jobs
MADA_SYNC_CRON=*/5 * * * *
VETOR_SYNC_CRON=*/30 * * * *
